import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="baseline"
  Component={Component}
  Source={Source}
  highlightLines="1"
  expand
  undent={2}
>
  The `baseline` property aligns the child elements to the baseline,
  equivalent to setting `align-items: baseline`.
</Example>
