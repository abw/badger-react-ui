import React        from 'react'
import Component    from './_examples/useWindow.jsx'
import Source       from './_examples/useWindow.jsx?raw'
import { Example } from '@abw/badger-website'

# useWindow

<div className="large">
  The hook can be used to monitor the width and height of the browser window.
</div>

As well as returning the window width and height in pixels it also returns
the breakpoint name corresponding to the width.

<Example
  Component={Component}
  code={Source}
  expand
/>
