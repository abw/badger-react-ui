import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { IconLink } from '@/web/site/Links.jsx'

<Example
  code="iconRight"
  Component={Component}
  Source={Source}
  highlightLines="4,10"
  undent={4}
  expand
>
  The `iconRight` property should be used when you want to render an icon to
  the right of some text.  It can be a string indicating the icon name or an
  object containing the `name` and any properties supported by the
  <IconLink/> component.
</Example>
