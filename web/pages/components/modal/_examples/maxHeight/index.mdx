import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="maxHeight"
  Component={Component}
  Source={Source}
  highlightLines="4"
  undent={6}
  expand
>
  The `maxHeight` property can be used as a shortcut to set the maximum height
  for the dialog.  Internally this adds a `--max-height` CSS custom property
  via a `style` attribute.
</Example>
