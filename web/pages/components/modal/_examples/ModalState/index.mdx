import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="ModalState"
  Component={Component}
  Source={Source}
  highlightLines="5"
  expand
>
  The `ModalState` function can be used to generate a Modal with state.  It
  returns the `isVisible` property, `show` and `hide` functions to change the
  visibility, and a `Modal` component.
</Example>
