import React      from 'react'
import Component  from './Light.jsx'
import Source     from './Light.jsx?raw'
import Example    from '@/page/ExampleSection.jsx'

<Example
  code="light"
  Component={Component}
  Source={Source}
  highlightLines="2"
  undent={8}
>
  The default overlay color is white in light mode and black in dark
  mode.  You can add the `light` property to explicitly set it
  to be light.
</Example>
