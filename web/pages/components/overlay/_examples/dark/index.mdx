import React      from 'react'
import Dark       from './Dark.jsx'
import DarkSrc    from './Dark.jsx?raw'
import Example    from '@/page/ExampleSection.jsx'

<Example
  code="dark"
  Component={Dark}
  Source={DarkSrc}
  highlightLines="2"
  caption="dark"
  undent={8}
>
  Add the `dark` property to explicitly set it to be dark.
</Example>
