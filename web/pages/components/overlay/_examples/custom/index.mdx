import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  title="Custom Colors"
  Component={Component}
  Source={Source}
  highlightLines="3-7"
  undent={8}
>
  You can set the `--color` and `--background-color` CSS properties to define
  your own colors for the overlay.  The `--background-opacity` can be set to
  change the background opacity.
</Example>
