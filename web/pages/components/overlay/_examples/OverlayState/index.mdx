import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="OverlayState"
  Component={Component}
  Source={Source}
  highlightLines="5"
  expand
>
  The `OverlayState` function can be used to generate an Overlay with state.
  It returns the `isVisible` property, `show` and `hide` functions to change
  the visibility, and an `Overlay` component.
</Example>
