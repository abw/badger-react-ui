import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="HorizontalSort"
  Component={Component}
  Source={Source}
>
  The `HorizontalSort` component works exactly the same as `Sortable` but
  constricts the items to be sorted horizontally. This is a shortcut for
  setting the `direction` property on `Sortable` to be `horizontal`.
</Example>
