import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="VerticalSort"
  Component={Component}
  Source={Source}
>
  The `VerticalSort` component works exactly the same as `Sortable` but
  constricts the items to be sorted vertically. This is a shortcut for
  setting the `direction` property on `Sortable` to be `vertical`.
</Example>
