import PropsTable  from '@/web/site/PropsTable.jsx'

### CSS Classes

The following properties can be used to change the CSS classes used
for different components and elements.

<PropsTable
  items={[
    ['className', '', 'Set a CSS class for the `details` container element.'],
    ['summaryClass', '', 'Add a CSS class to the `summary` element (added to `detailsSummaryClass`).'],
    ['detailsSummaryClass', 'summary', 'The pre-defined CSS class for the `summary` element.'],
    ['contentClass', '', 'Set a CSS class for the content `div` element.'],
  ]}
/>
