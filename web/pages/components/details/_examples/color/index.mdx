import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="color"
  Component={Component}
  Source={Source}
  highlightLines="4,12"
  undent={4}
  expand
>
  The `color` property can be used to set the color range. It is most commonly
  used in conjunction with the `border`, `lined` and `shaded` properties.
</Example>
