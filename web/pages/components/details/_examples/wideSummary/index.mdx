import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="wideSummary"
  Component={Component}
  Source={Source}
  highlightLines="6"
  undent={2}
  expand
>
  The `wideSummary` property can be set to make the summary text expand to
  the full width of the container.  It't useful if you want the `iconRight`
  to be way over on the right, or if you have a custom `summary` with some
  detail you want displayed on the right.
</Example>
