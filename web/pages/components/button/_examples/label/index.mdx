import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="label"
  Component={Component}
  Source={Source}
  highlightLines="3"
  undent={2}
  expand
>
  The `label` property can be used to set the `aria-label` property on the
  button.  This typically doesn't have any visual effect on the button, but
  is used by screen readers to provide more detail about the functionality
  of the button.
</Example>

