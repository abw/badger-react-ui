import React       from 'react'
import Component   from './Component'
import Source      from './Component.tsx?raw'
import { Example } from '@abw/badger-website'

<div className="large">
  The `Button` component can be used to render a button.
  Any child elements will be included as content.
</div>

There are numerous properties listed below that are typically used as
shortcuts to set particular CSS styles.  Any other properties not listed
here are forwarded to the `button` element.

<Example
  Component={Component}
  code={Source}
  expand
/>
