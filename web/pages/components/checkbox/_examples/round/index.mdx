import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="round"
  Component={Component}
  Source={Source}
  highlightLines="5"
  undent={4}
  expand
>
  The `round` property can be used in conjunction with the `switch` property
  to render the checkbox as a round switch.
</Example>
