import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="inputClass"
  Component={Component}
  Source={Source}
  highlightLines="3"
  undent={4}
  expand
>
  The `inputClass` property can be used to add a CSS class to the checkbox
  `input` element.
</Example>
