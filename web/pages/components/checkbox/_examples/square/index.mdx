import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="square"
  Component={Component}
  Source={Source}
  highlightLines="3"
  undent={4}
  expand
>
  The `square` property can be used in conjunction with the `switch` property
  to render the checkbox as a square switch.
</Example>
