import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { CodeHashLink } from '@/web/site/Links.jsx'

<Example
  code="alignInput"
  Component={Component}
  Source={Source}
  highlightLines="8,13,18"
  undent={4}
  expand
>
  The `alignInput` property (or `alignInputs` as an alias) can be specified
  specified to set the alignment of the input.

  The default value is `center` which will aligns the input to the center.
  It can also be set to `left` or `right`.

  This option only has an effect when <CodeHashLink id="showInput"/> is set.
</Example>
