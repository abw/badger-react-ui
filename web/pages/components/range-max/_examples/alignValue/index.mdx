import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { CodeHashLink } from '@/web/site/Links.jsx'

<Example
  code="alignValue"
  Component={Component}
  Source={Source}
  highlightLines="8,13,18"
  undent={4}
  expand
>
  The `alignValue` property can be set to specify the alignment of the
  current value.

  The default value is `thumb` which will align the value with the draggable
  "thumb".  It can also be set to `left`, `center` or `right`.

  This option only has an effect when <CodeHashLink id="showValue"/> is set.
</Example>
