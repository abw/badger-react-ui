import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="icon"
  Component={Component}
  Source={Source}
  highlightLines="3"
  undent={2}
  expand
>
  The `icon` property can used to define an icon for the trigger.  The
  `iconClass` can be used to add a CSS class for it.
</Example>
