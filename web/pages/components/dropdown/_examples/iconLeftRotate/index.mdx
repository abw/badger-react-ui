import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="iconLeftRotate"
  Component={Component}
  Source={Source}
  highlightLines="5"
  undent={2}
  expand
>
  The `iconLeftRotate` property can used to specify the rotation (in degrees)
  of the icon when the dropdown is open.
</Example>
