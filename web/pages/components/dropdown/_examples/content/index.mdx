import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="content"
  Component={Component}
  Source={Source}
  highlightLines="3"
  undent={2}
  expand
>
  The `content` property can be used to define the content as a short-cut.
  Otherwise any child content of the `Dropdown` component will be displayed.
</Example>
