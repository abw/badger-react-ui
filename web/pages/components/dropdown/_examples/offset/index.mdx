import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="offset"
  Component={Component}
  Source={Source}
  highlightLines="4"
  undent={2}
  expand
>
  The `offset` property can added to set the offset (in pixels) of the
  dropdown body from the trigger.  The default value is 6.
</Example>
