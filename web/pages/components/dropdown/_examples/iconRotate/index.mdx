import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="iconRotate"
  Component={Component}
  Source={Source}
  highlightLines="4"
  undent={2}
  expand
>
  The `iconRotate` property can used to specify the rotation (in degrees) of
  the icon when the dropdown is open.
</Example>
