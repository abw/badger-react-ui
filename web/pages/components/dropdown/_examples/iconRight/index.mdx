import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="iconRight"
  Component={Component}
  Source={Source}
  highlightLines="4"
  undent={2}
  expand
>
  The `iconRight` property can used to define an icon for the left side of
  the trigger.  The `iconRightClass` can be used to add a CSS class for it.
</Example>
