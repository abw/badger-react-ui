import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="iconLeft"
  Component={Component}
  Source={Source}
  highlightLines="4"
  undent={2}
  expand
>
  The `iconLeft` property can used to define an icon for the left side of the
  trigger.  The `iconLeftClass` can be used to add a CSS class for it.
</Example>
