import React            from 'react'
import Dropdown         from './_examples/Dropdown/index.mdx'
import Text             from './_examples/text/index.mdx'
import Content          from './_examples/content/index.mdx'
import OnHover          from './_examples/openOnHover/index.mdx'
import Right            from './_examples/right/index.mdx'
import Button           from './_examples/button/index.mdx'
import Wide             from './_examples/wide/index.mdx'
import Placement        from './_examples/placement/index.mdx'
import Offset           from './_examples/offset/index.mdx'
import Icon             from './_examples/icon/index.mdx'
import IconRotate       from './_examples/iconRotate/index.mdx'
import IconLeft         from './_examples/iconLeft/index.mdx'
import IconLeftRotate   from './_examples/iconLeftRotate/index.mdx'
import IconRight        from './_examples/iconRight/index.mdx'
import IconRightRotate  from './_examples/iconRightRotate/index.mdx'
import PropsTable       from '@/web/site/PropsTable.jsx'
import { SubHeading } from '@abw/badger-website'

# Dropdown

<Dropdown/>

## Properties

<Text/>
<Content/>
<OnHover/>
<Right/>
<Wide/>
<Placement/>
<Offset/>
<Icon/>
<IconRotate/>
<IconLeft/>
<IconLeftRotate/>
<IconRight/>
<IconRightRotate/>
<Button/>

## CSS Classes

The following properties can be provided to set or modify the CSS classes
used in various elements.

<PropsTable
  items={[
    ['className', 'dropdown', 'The CSS class for the dropdown container element.'],
    ['size', '', 'Used to add a size to the CSS class added to the dropdown container element.'],
    ['openClass', 'open', 'CSS class added to the dropdown container element in the open state.'],
    ['closedClass', 'closed', ' CSS class added to the dropdown container element in the closed state.'],
    ['triggerClass', 'trigger', 'The CSS class added to the dropdown trigger element.'],
    ['triggerColor', '', 'Used to add a color to the CSS class for the dropdown trigger.'],
    ['triggerSize', '', 'Used to add a size to the CSS class for the dropdown trigger.'],
    ['bodyClass', 'body border bdr-1', 'The CSS class added to the dropdown menu container. The `border` utility class adds a border and `bdr-1` sets the border radius to 1 unit (0.25rem or 4px).'],
    ['safeAreaClass', 'safe-area', 'CSS class for the safe area added between the trigger and body'],
  ]}
  />

## Components

<SubHeading code="Trigger" id="Trigger-component"/>

Renders the dropdown menu trigger.  Defaults
to `DropdownTrigger`.

<SubHeading code="Body" id="Body-component"/>

Renders the dropdown body.  Defaults
to `DropdownBody`.
