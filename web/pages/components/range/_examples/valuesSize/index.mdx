import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="valuesSize"
  Component={Component}
  Source={Source}
  highlightLines="4"
  undent={2}
  expand
>
  The `valuesSize` property (or `valueSize` as an alias) can be specified to
  set the size for the displayed value(s). It can be set to any of the
  standard sizes: `smallest`, `smaller`, `small`, `medium`, `large`, `larger`
  or `largest`. The default value is `small`.
</Example>
