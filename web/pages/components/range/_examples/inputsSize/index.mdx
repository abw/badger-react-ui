import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { CodeHashLink } from '@/web/site/Links.jsx'

<Example
  code="inputsSize"
  Component={Component}
  Source={Source}
  highlightLines="8,13,18"
  undent={4}
  expand
>
  The `inputsSize` property (or `inputSize` as an alias) can be specified to
  set the size of the inputs. It can be set to any of the standard sizes:
  `smallest`, `smaller`, `small`, `medium`, `large`, `larger` or `largest`.

  This option only has an effect when <CodeHashLink id="showInputs"/> is set.
</Example>
