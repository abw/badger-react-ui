import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { CodeHashLink } from '@/web/site/Links.jsx'

<Example
  code="tickStep"
  Component={Component}
  Source={Source}
  undent={2}
  highlightLines="4"
  expand
>
  The `tickStep` property allows you to set a step for the displayed ticks
  which is different to the main <CodeHashLink id="step"/> property which
  controls the granularity of the slider.
</Example>
