import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="maxRange"
  Component={Component}
  Source={Source}
  undent={2}
  highlightLines="4"
  expand
>
  The `maxRange` property can be used to define a maximum permissable range
  between the `minValue` and `maxValue`.

  No matter how hard you try to cleave apart the ends of the the range, they
  won't budge if the difference between them would be more than the limit
  imposed by `maxRange`.
</Example>
