import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="strokeStop"
  Component={Component}
  Source={Source}
  undent={2}
  expand
>
  The `strokeStop` property can be used to set the color stop for the stroke,
  ranging from 0 (near black) to 100 (near white) in steps of 5.  The
  `strokeStopDark` property can be used to set the color stop for dark mode.
</Example>
