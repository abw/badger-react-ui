import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { CodeHashLink } from '@/site/Links.jsx'

<Example
  code="bgStroke"
  Component={Component}
  Source={Source}
  undent={2}
  expand
>
  The `bgStroke` property can be used to set the stroke width of a background
  icon.

  It only has any effect when <CodeHashLink id="bgIcon"/> is set.

  It can be set to `true` to get the default stroke width, or one of the
  semantic names: `thinnest`, `thinner`, `thin`, `thick`, `thicker` or
  `thickest`.
</Example>
