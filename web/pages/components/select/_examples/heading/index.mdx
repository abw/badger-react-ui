import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="heading"
  Component={Component}
  Source={Source}
  highlightLines="5,9"
  expand
>
  An element in the `options` array can be an object with a `heading` property
  to denote a heading item.
</Example>
