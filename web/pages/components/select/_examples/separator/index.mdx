import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="separator"
  Component={Component}
  Source={Source}
  highlightLines="8"
  expand
>
  An element in the `options` array can be an object with a `separator`
  property to render a separator.
</Example>
