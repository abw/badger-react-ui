import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="indent"
  Component={Component}
  Source={Source}
  highlightLines="5-13"
  expand
>
  An element in the `options` array can have an `indent` number to indent it.
  The number can be from 1 to 4.
</Example>
