import React          from 'react'
import Select         from './_examples/Select/index.mdx'
import Options        from './_examples/options/index.mdx'
import Headings       from './_examples/heading/index.mdx'
import Value          from './_examples/value/index.mdx'
import DisplayOption  from './_examples/displayOption/index.mdx'
import DisplayValue   from './_examples/displayValue/index.mdx'
import ValidOption    from './_examples/validOption/index.mdx'
import Search         from './_examples/search/index.mdx'
import SearchOptions  from './_examples/searchOptions/index.mdx'
import Disabled       from './_examples/disabled/index.mdx'
import Separator      from './_examples/separator/index.mdx'
import Indent         from './_examples/indent/index.mdx'
import { CodeHashLink } from '@/web/site/Links.jsx'
import { Heading, Section } from '@abw/badger-website'

# Select

<Select/>

<Heading title="Properties"/>
<Options/>
<Value/>
<DisplayOption/>
<DisplayValue/>
<ValidOption/>
<Search/>
<SearchOptions/>
<Disabled/>

<Section code="placeholder" split>
  Set the placeholder text.  The default is `Select`.
</Section>

<Section code="noOptions" split>
  Set the text displayed when there are no options to select from.
  The default is `No options`.
</Section>

<Section code="openOnHover" split>
  Flag to indicate that the option menu should open when the cursor
  hovers over the trigger input.  Defaults to `false`.
</Section>

<Section code="closeOnBlur" split>
  Flag to indicate that the option menu should close when focus is lost.
  Defaults to `true`.
</Section>

<Section code="closeDelay" split>
  Delay (in milliseconds) before the menu is closed.  This is required
  to ensure that any click events have been registered before the menu
  is closed. Defaults to `300`.
</Section>

<Heading title="Option Properties"/>
<Headings/>
<Separator/>
<Indent/>

<Heading title="Event Handlers"/>
<Section code="onLoad">
  Called when the component is first loaded.  The handler is passed a
  reference to the `SearchContext` object.
</Section>
<Section code="onUnload">
  Called when the component is unloaded.  The handler is passed a
  reference to the `SearchContext` object.
</Section>
<Section code="onFocus">
  Called when the component gains focus.
</Section>
<Section code="onBlur">
  Called when the component loses focus.
</Section>
<Section code="onClick">
  Called when the component is clicked.
</Section>
<Section code="onOpen">
  Called when the component is opened and selectable options are displayed.
</Section>
<Section code="onClose">
  Called when the component is closed and selectable options are hidden.
</Section>
<Section code="onSelect">
  Called when an option is selected.  The selected option is passed as an
  argument.
</Section>
<Section code="onUpdate">
  If this is defined it will be called instead of any
  <CodeHashLink id="onSelect"/> handler when the selection changes due to
  the <CodeHashLink id="options"/> or <CodeHashLink id="value"/> properties
  changing.
</Section>

<Heading title="CSS Classes"/>
<Section code="placeholderClass">
  Set the class for the placeholder text.
  The default is `placeholder`.
</Section>
<Section code="inputClass">
  Set the class for the input element.
  The default is `input`.
</Section>
<Section code="selectingClass">
  Set the class added when a selection is in place.
  The default is `selecting`.
</Section>
<Section code="optionsClass">
  Set the class added for the options container.
  The default is `menu border bdr-1`.
</Section>
<Section code="optionClass">
  Set the class added to each option element.
  The default is `item`.
</Section>
<Section code="activeClass">
  Set the class added to an option that is currently active.
  The default is `active`.
</Section>
<Section code="noOptionsClass">
  Set the class added to the element displayed when there are no
  options to select from. The default is `none`.
</Section>

<Heading title="Components"/>
<Section code="Content">
  Renders the content of the select component.
</Section>
<Section code="Input">
  Renders the input field.
</Section>
<Section code="Options" id="Options-component">
  Renders the selectable options.
</Section>
<Section code="Option">
  Renders an individual option.
</Section>
<Section code="NoOptions" id="NoOptions-component">
  Renders a message indicating that there are no options to select from.
</Section>
