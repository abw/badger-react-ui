import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="prompt"
  Component={Component}
  Source={Source}
  highlightLines="5,12-19"
  undent={4}
  expand
>
  The `prompt` property can be used to provide an additional prompt to the
  user in a modal confirmation. If a `prompt` is defined then the buttons
  will be rendered in the modal footer.  Otherwise they will be in the main
  modal body.
</Example>
