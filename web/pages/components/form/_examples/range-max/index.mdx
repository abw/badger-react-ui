import React      from 'react'
import Simple     from './Simple.jsx'
import SimpleSrc  from './Simple.jsx?raw'
import { RangeMaxLink } from '@/web/site/Links.jsx'
import { Example, Section } from '@abw/badger-website'

<Section code="range-max">
  The field `type` property can be set to `range-max`
  (or `rangemax`, `rangeMax`, etc.) to use
  the <RangeMaxLink/> component.

  <Example
    Component={Simple}
    code={SimpleSrc}
    highlightLines="5"
  />
</Section>
