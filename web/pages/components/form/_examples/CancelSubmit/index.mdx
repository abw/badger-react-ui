import React       from 'react'
import Classes     from './Classes.jsx'
import ClassesSrc  from './Classes.jsx?raw'
import { Example, Section } from '@abw/badger-website'

<Section code="CancelSubmit">
  The `CancelSubmit` component is a wrapper around the
  equivalent component from Badger Form.  It allows you to define icons
  for buttons.

  <Example
    Component={Classes}
    code={ClassesSrc}
    highlightLines="18-29"
  />
</Section>
