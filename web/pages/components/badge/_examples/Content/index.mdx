import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'
import { WithIconsLink } from '@/site/Links.jsx'

<Example
  code="Content"
  Component={Component}
  Source={Source}
  highlightLines="3-8"
  undent={2}
  expand
>
  The `Content` property (note the capital C) can be used to provide an
  alternate component to render the badge content. By default it uses
  <WithIconsLink/> to render the badge text with optional icons.
</Example>

