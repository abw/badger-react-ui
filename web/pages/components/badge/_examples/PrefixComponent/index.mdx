import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { WithIconsLink } from '@/site/Links.jsx'

<Example
  code="Prefix"
  id="prefix-component"
  Component={Component}
  Source={Source}
  highlightLines="4-9"
  undent={2}
  expand
>
  The `Prefix` property (note the capital P) can be used to provide an
  alternate component to render any prefix.
</Example>

