import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="tooltip"
  Component={Component}
  Source={Source}
  highlightLines="3-4"
  undent={2}
  expand
>
  The `tooltip` property can be used to set the `data-tooltip` property on the
  badge.  This can be used to indicate the direction of the tooltip, e.g.
  `top`, `right`, `top right`, etc. The tooltip content should be defined
  using the `label` property.
</Example>

