import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="size"
  Component={Component}
  highlightLines={ line => line % 4 === 0 }
  Source={Source}
  undent={2}
>
  The `size` property can be used to set the badge size. This can be one of:
  `smallest`, `smaller`, `small`, `medium` (default - same as no explicit
  size), `large`, `larger`, `largest`; or any of the size multipliers: `x2`,
  `x3`, etc., up to `x10`.
</Example>

