import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { DetailsLink } from '@/site/Links.jsx'

<Example
  code="items"
  Component={Component}
  Source={Source}
  highlightLines="3-21"
  undent={2}
  expand
>
  The `items` property should be used to define an array of items to appear in
  the accordion.

  Each item should define a `summary` and main `content`, which can either be
  strings or JSX fragments.  Any other properties will be forwarded on to the
  <DetailsLink/> component.
</Example>
