import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="size"
  Component={Component}
  Source={Source}
  undent={2}
  expand
>
  The `size` property is a short-cut to set the size.  This adds the size to
  the CSS class.
</Example>
