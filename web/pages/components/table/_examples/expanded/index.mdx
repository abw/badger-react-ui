import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="expanded"
  Component={Component}
  Source={Source}
  highlightLines="2"
  expand
  undent={2}
>
  The `expanded` property is a shortcut to add the `expanded` CSS class which
  increases the padding in each cell.
</Example>
