import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="shaded"
  Component={Component}
  Source={Source}
  highlightLines="2"
  expand
  undent={2}
>
  The `shaded` property is a shortcut to add the `shaded` CSS class which
  adds background colors to each cell.
</Example>
