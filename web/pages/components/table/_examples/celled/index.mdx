import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="celled"
  Component={Component}
  Source={Source}
  highlightLines="2"
  expand
  undent={2}
>
  The `celled` property is a shortcut to add the `celled` CSS class which
  adds border lines to each cell.
</Example>
