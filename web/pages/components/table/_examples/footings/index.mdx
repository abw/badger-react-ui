import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="footings"
  Component={Component}
  Source={Source}
  highlightLines="12-15,22"
  expand
>
  The `footings` property is a shortcut for the common case where you just
  want to have a single row in the footer.  It is equivalent to defining
  `footRows` with the `footings` as the only element.
</Example>
