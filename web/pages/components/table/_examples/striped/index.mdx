import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="striped"
  Component={Component}
  Source={Source}
  highlightLines="2"
  expand
  undent={2}
>
  The `striped` property is a shortcut to add the `striped` CSS class which
  adds striped background colors to each cell.
</Example>
