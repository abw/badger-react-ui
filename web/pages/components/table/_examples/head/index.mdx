import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="head"
  Component={Component}
  Source={Source}
  highlightLines="3-11,23"
  expand
>
  The `head` property is used to define an array of rows for the table
  header.  The data format is the same as for `body`.
</Example>
