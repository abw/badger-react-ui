import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="foot"
  Component={Component}
  Source={Source}
  highlightLines="6-13,27"
  expand
>
  The `foot` property is used to define an array of rows for the table
  footer.  The data format is the same as for `body`.
</Example>
