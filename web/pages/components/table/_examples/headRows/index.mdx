import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="headRows"
  Component={Component}
  Source={Source}
  highlightLines="4-12,24"
  expand
>
  The `headRows` property is used to define an array of rows for the table
  header.  The data format is the same as for `rows` / `bodyRows`.
</Example>
