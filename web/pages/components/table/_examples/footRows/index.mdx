import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="footRows"
  Component={Component}
  Source={Source}
  highlightLines="7-14,28"
  expand
>
  The `footRows` property is used to define an array of rows for the table
  footer.  The data format is the same as for `rows` / `bodyRows`.
</Example>
