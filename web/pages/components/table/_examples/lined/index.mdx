import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="lined"
  Component={Component}
  Source={Source}
  highlightLines="2"
  expand
  undent={2}
>
  The `lined` property is a shortcut to add the `lined` CSS class which adds
  border lines between each row.
</Example>
