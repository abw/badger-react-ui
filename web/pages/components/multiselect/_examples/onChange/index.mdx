import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="onChange"
  Component={Component}
  Source={Source}
  highlightLines="4-5,10"
  expand
>
  The `onChange` property can be used to define a handler which will be called
  whenever an item is selected or unselected.  The updated list of selected
  options will be passed as an argument.
</Example>
