import React   from 'react'
import RowsSrc from '../animals.js?raw'
import { Source, SubSection }  from '@abw/badger-website'

<SubSection code="rows">
  The `rows` property is used to provide the data that you want to display.
  It should be an array of objects.  The object can contain any data.

  In these examples we're using this data for the `rows` unless otherwise
  shown.
  <Source
    code={RowsSrc}
    language="js"
  />
</SubSection>
