import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { BadgerUtilsLink } from '@/site/Links.jsx'

<Example
  code="number"
  Component={Component}
  Source={Source}
  undent={2}
  highlightLines="8"
  expand
>
  The `type` can be set to `number` for numerical columns.  This will use a
  numerical comparison when sorting rows on this column and format the value
  for display using the <BadgerUtilsLink path="numbers.html#formatNumber"
  code="formatNumber()"/> function from <BadgerUtilsLink/>.
</Example>
