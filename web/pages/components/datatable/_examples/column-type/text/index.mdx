import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="text"
  Component={Component}
  Source={Source}
  undent={2}
  highlightLines="8"
  expand
>
  The default column `type` is `text`.  You don't need to specify it but you
  can if you like being explicit.
</Example>
