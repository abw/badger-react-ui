import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'
import { BadgerUtilsLink } from '@/site/Links.jsx'

<Example
  code="currency"
  Component={Component}
  Source={Source}
  undent={2}
  highlightLines="9"
  expand
>
  The `type` can be set to `currency` for financial
  values. This will use a numerical comparison when sorting rows on this
  column and format the value for display using the
  <BadgerUtilsLink path="number.html#currency" code="currency()"/> function
  from <BadgerUtilsLink/>.
</Example>
