import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="filterable"
  Component={Component}
  Source={Source}
  highlightLines="6"
  undent={2}
  expand
>
  The `filterable` property can be set to `false` to prevent the column from
  being filtered.
</Example>
