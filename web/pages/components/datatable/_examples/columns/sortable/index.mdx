import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="sortable"
  Component={Component}
  Source={Source}
  highlightLines="6"
  undent={2}
  expand
>
  The `sorted` property can be set to `false` to prevent the column from
  being used to sort the rows.
</Example>
