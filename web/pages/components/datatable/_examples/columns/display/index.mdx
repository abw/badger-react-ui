import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="display"
  Component={Component}
  Source={Source}
  highlightLines="6-7"
  undent={2}
  expand
>
  The `display` property can be used to define a custom function to display
  the column value.  It will be passed an object containing the `row`,
  `column`, `name`, `value`, `rowIndex` and `cellIndex`.
</Example>
