import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="solid"
  Component={Component}
  Source={Source}
  highlightLines="2,9"
  expand
  undent={4}
>
  The `solid` property can be used to render the tabs using solid colors.
  It can be used in conjunction with `lined`.
</Example>
