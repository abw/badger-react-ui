import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="activeTab"
  Component={Component}
  Source={Source}
  highlightLines="2"
  expand
  undent={2}
>
  The `activeTab` property can be used to set the active tab. It should be an
  integer representing the index in `tabs`, e.g. from `0` to `tabs.length - 1`.
</Example>
