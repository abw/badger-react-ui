import Tabset     from './_examples/Tabset/index.mdx'
import Tabs       from './_examples/Tabs/index.mdx'
import ActiveTab  from './_examples/ActiveTab/index.mdx'
import StorageKey from './_examples/StorageKey/index.mdx'
import Lined      from './_examples/Lined/index.mdx'
import Solid      from './_examples/Solid/index.mdx'
import Color      from './_examples/Color/index.mdx'
import Size       from './_examples/Size/index.mdx'
import Center     from './_examples/Center/index.mdx'
import Right      from './_examples/Right/index.mdx'
import { Heading } from '@abw/badger-website'

# Tabset

<Tabset/>

## Properties

<Tabs/>
<ActiveTab/>
<StorageKey/>
<Lined/>
<Solid/>
<Color/>
<Size/>
<Center/>
<Right/>

## CSS Classes

### `tabsetClass`

The CSS class added to the tabset container.  This defaults to `tabset`.

### `tabsClass`

The CSS class added to the tabs list.  This defaults to `tabs`.

### `activeClass`

The CSS class added to the active tab.  This defaults to `active`.

### `disabledClass`

The CSS class added to a disabled active tab.  This defaults to `disabled`.

### `className`

An additional CSS class to be added to the tabset container.  Use this for
applying additional margin utility classes, for example.

### `bodyClass`

A CSS class to be added to the body container.

### `Body`

A component to render the body for each tab.  It will be passed the active
`tab` as a property.

