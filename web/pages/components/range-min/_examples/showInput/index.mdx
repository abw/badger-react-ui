import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="showInput"
  Component={Component}
  Source={Source}
  undent={2}
  highlightLines="3"
  expand
>
  The `showInput` property (or `showInputs` as an alias) can be specified to
  display an input below the slider to allow precise values to be entered.
</Example>
