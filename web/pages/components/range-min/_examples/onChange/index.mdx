import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="onChange"
  Component={Component}
  Source={Source}
  highlightLines="5-8,15"
  expand
>
  The `onChange` property can be set as a handler function which will be
  called when the `value` (AKA `minValue`) changes.  The function will be
  called with the new `value` passed as an argument.  The second argument
  will be the context state object.
</Example>
