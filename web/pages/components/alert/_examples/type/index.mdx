import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="type"
  Component={Component}
  Source={Source}
  highlightLines={ line => line % 4 === 0 }
  undent={2}
  expand
>
  The `type` property can be used to set it to one of the pre-defined types:
  `info`, `success`, `warning` or `error`.
</Example>

