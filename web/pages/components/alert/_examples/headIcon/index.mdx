import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="headIcon"
  Component={Component}
  Source={Source}
  highlightLines="3"
  undent={2}
  expand
>
  The `headIcon` property can be used to add an icon to the headline.  The
  `headicon` (note the lower case "i") is provided as an alias because there's
  at least one user (me) who forgets that it's supposed to have a capital "I"
  in the middle.
</Example>

