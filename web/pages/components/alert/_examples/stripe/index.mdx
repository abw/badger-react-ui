import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'

<Example
  code="stripe"
  Component={Component}
  Source={Source}
  highlightLines="5,11,17,23,29"
  undent={2}
  expand
>
  The `stripe` property can be set as a short-cut to add the `stripe` CSS
  class for a more minimal look.
</Example>

