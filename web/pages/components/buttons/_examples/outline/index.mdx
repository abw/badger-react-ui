import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component'
import Source     from './Component.tsx?raw'
import { ButtonLink } from '@/web/site/Links.jsx'

<Example
  code="outline"
  Component={Component}
  Source={Source}
  highlightLines="2"
  undent={4}
  expand
>
  Additional properties passed to the `Buttons` component will be forwarded
  to each <ButtonLink/> component.  For example, you can set the `outline`
  property on the parent `Buttons` component so that you don't have to repeat
  it for each button.  However, individual buttons can override any of these
  properties.
</Example>
