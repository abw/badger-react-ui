import Toggle        from './_examples/Toggle/index.mdx'
import Options       from './_examples/options/index.mdx'
import Selected      from './_examples/selected/index.mdx'
import OnSelect      from './_examples/onSelect/index.mdx'
import FindSelected  from './_examples/findSelectedIndex/index.mdx'
import Color         from './_examples/color/index.mdx'
import Size          from './_examples/size/index.mdx'
import ActiveProps   from './_examples/activeProps/index.mdx'
import InactiveProps from './_examples/inactiveProps/index.mdx'
import ToggleState   from './_examples/ToggleState/index.mdx'
import UseToggle     from './_examples/useToggle/index.mdx'

# Toggle

<Toggle/>

## Properties

<Options/>
<Selected/>
<FindSelected/>
<Size/>
<Color/>
<ActiveProps/>
<InactiveProps/>

## Event Handlers

<OnSelect/>

## Components

<ToggleState/>
<UseToggle/>
{/*
<Section code="tabsetClass">
The CSS class added to the tabset container.  This defaults
to `tabset`.
</Section>
<Section code="tabsClass">
The CSS class added to the tabs list.  This defaults
to `tabs`.
</Section>
<Section code="activeClass">
The CSS class added to the active tab.  This defaults
to `active`.
</Section>
<Section code="disabledClass">
The CSS class added to a disabled active tab.  This defaults
to `disabled`.
</Section>
<Section code="className">
An additional CSS class to be added to the tabset container.  Use
this for applying additional margin utility classes, for example.
</Section>
<Section code="bodyClass">
A CSS class to be added to the body container.
</Section>
<Section code="Body">
A component to render the body for each tab.  It will be passed
the active `tab` as a property.
</Section>
*/}
