import React      from 'react'
import Example    from '@/page/ExampleSection.jsx'
import Component  from './Component.jsx'
import Source     from './Component.jsx?raw'

<Example
  code="clear"
  Component={Component}
  Source={Source}
  highlightLines="39-42"
>
  You can use the `onLoad` property to save a reference to the search context.
  You can call the `clear` method on that to programmatically clear the input.
  This has the same effect as clicking on the cross icon to the right of the
  input.
</Example>
